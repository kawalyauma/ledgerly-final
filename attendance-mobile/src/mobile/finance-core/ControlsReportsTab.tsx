import {useState}from"react";import{ScrollView,StyleSheet,Text,TouchableOpacity,View}from"react-native";import type{MobileSession}from"../auth";import type{SessionUpdater}from"../apiClient";import{BudgetsPanel}from"./controls/BudgetsPanel";import{PeriodsPanel}from"./controls/PeriodsPanel";import{InventoryPanel}from"./controls/InventoryPanel";import{TaxPanel}from"./controls/TaxPanel";import{ReportsPanel}from"./controls/ReportsPanel";
type Mode="periods"|"budgets"|"inventory"|"tax"|"reports";const ITEMS:[Mode,string][]=[["periods","Periods"],["budgets","Budgets"],["inventory","Inventory"],["tax","Tax"],["reports","Reports"]];
export function ControlsReportsTab({session,onSession}:{session:MobileSession;onSession:SessionUpdater}){const[mode,setMode]=useState<Mode>("periods");return <View style={s.root}><ScrollView horizontal showsHorizontalScrollIndicator={false} contentContainerStyle={s.nav}>{ITEMS.map(([id,label])=><TouchableOpacity key={id} onPress={()=>setMode(id)} style={[s.item,mode===id&&s.on]}><Text style={[s.text,mode===id&&s.textOn]}>{label}</Text></TouchableOpacity>)}</ScrollView><View style={s.body}>{mode==="periods"?<PeriodsPanel session={session} onSession={onSession}/>:mode==="budgets"?<BudgetsPanel session={session} onSession={onSession}/>:mode==="inventory"?<InventoryPanel session={session} onSession={onSession}/>:mode==="tax"?<TaxPanel session={session} onSession={onSession}/>:<ReportsPanel session={session} onSession={onSession}/>}</View></View>}
const s=StyleSheet.create({root:{flex:1},nav:{paddingHorizontal:12,paddingVertical:8,gap:7},item:{paddingHorizontal:13,paddingVertical:8,borderRadius:18,backgroundColor:"#edf1ef"},on:{backgroundColor:"#193d31"},text:{fontSize:9.5,fontWeight:"900",color:"#60766c"},textOn:{color:"white"},body:{flex:1,paddingHorizontal:16}});