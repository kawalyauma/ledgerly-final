import{MobileApiError}from"../auth";import type{CameraRow,ServerRow}from"./types";
export function err(e:unknown){return e instanceof MobileApiError?e.message:e instanceof Error?e.message:"Something went wrong."}export function tone(v:string){const s=String(v||"").toLowerCase();if(["online","ready","healthy","active","reviewed","completed","acknowledged"].includes(s))return"good" as const;if(["warning","degraded","recording","open","claimed","scanning","uploading"].includes(s))return"warn" as const;if(["critical","failed","offline","revoked","dismissed","error","tamper"].includes(s))return"bad" as const;return"neutral" as const}
export function bytes(n:number){if(!n)return"0 B";const u=["B","KB","MB","GB","TB"];let i=0;while(n>=1024&&i<u.length-1){n/=1024;i++}return`${n.toFixed(i<2?0:1)} ${u[i]}`}
export function localDateTime(d=new Date()){const x=new Date(d.getTime()-d.getTimezoneOffset()*60000);return x.toISOString().slice(0,16)}export function isoLocal(v:string){return new Date(v).toISOString()}
export function duration(ms:number){const s=Math.max(0,Math.round(ms/1000));if(s<60)return`${s}s`;if(s<3600)return`${Math.round(s/60)}m`;return`${(s/3600).toFixed(1)}h`}
export const cameraOptions=(rows:CameraRow[])=>rows.map(x=>({label:x.name,value:x.id,detail:[x.location,x.status,x.stream_status].filter(Boolean).join(" · ")}));export const serverOptions=(rows:ServerRow[])=>[{label:"Not assigned",value:""},...rows.map(x=>({label:x.name,value:x.id,detail:[x.location,x.status,x.media_status].filter(Boolean).join(" · ")}))];
export function eventLabel(v:string){return String(v||"").replaceAll("_"," ").replace(/\b\w/g,m=>m.toUpperCase())}
