import type{MobileSession}from"../auth";import{ledgerlyRequest,query,type SessionUpdater}from"../apiClient";import type{CameraDiagnostics,CameraOverview,CameraPairing,CameraProfile,CameraRow,DeploymentReadiness,EventConfig,EventRow,FleetAdmin,FleetHealthSummary,GovernanceData,Incident,LiveSession,NotificationProviders,NotificationQueueRow,Operations,PlaybackGrant,Recording,RecoveryDrill,RedundancyData,SecurityAuditRow,SecurityInboxNotice,ServerPairing,ServerRow,ServerUpdatePolicy,Timeline}from"./types";
type Client={session:MobileSession;onSession?:SessionUpdater};const req=<T>(c:Client,path:string,init:RequestInit={})=>ledgerlyRequest<T>(c.session,path,init,c.onSession);const json=(method:string,body?:unknown):RequestInit=>({method,headers:{"Content-Type":"application/json"},body:body===undefined?undefined:JSON.stringify(body)});
export const securityCameraApi={manifest:(c:Client)=>req<any>(c,"/security-camera/manifest"),overview:(c:Client)=>req<CameraOverview>(c,"/security-camera/overview"),healthSummary:(c:Client)=>req<FleetHealthSummary>(c,"/security-camera/health-summary"),cameras:(c:Client)=>req<CameraRow[]>(c,"/security-camera/cameras"),servers:(c:Client)=>req<ServerRow[]>(c,"/security-camera/servers"),createPairing:(c:Client,body:{name:string;location?:string|null;serverId?:string|null})=>req<CameraPairing>(c,"/security-camera/pairings",json("POST",body)),createServerPairing:(c:Client,body:{name:string;location?:string|null})=>req<ServerPairing>(c,"/security-camera/server-pairings",json("POST",body)),fleet:(c:Client)=>req<FleetAdmin>(c,"/security-camera/fleet"),saveGroup:(c:Client,body:any)=>req<any>(c,"/security-camera/groups",json("POST",body)),saveWallView:(c:Client,body:any)=>req<any>(c,"/security-camera/wall-views",json("POST",body)),setLifecycle:(c:Client,cameraId:string,body:{status:string;note?:string|null})=>req<any>(c,`/security-camera/cameras/${cameraId}/lifecycle`,json("POST",body)),diagnostics:(c:Client,cameraId:string)=>req<CameraDiagnostics>(c,`/security-camera/cameras/${cameraId}/diagnostics`),snapshot:(c:Client,cameraId:string)=>req<{id:string;cameraId:string;cameraName:string;expiresAt:string;url:string}>(c,`/security-camera/cameras/${cameraId}/snapshot`,json("POST",{})),governance:(c:Client)=>req<GovernanceData>(c,"/security-camera/governance"),audit:(c:Client,p:{actor?:string;action?:string;cameraId?:string;from?:string;to?:string;limit?:number}={})=>req<SecurityAuditRow[]>(c,`/security-camera/audit${query({...p,limit:p.limit||300})}`),saveNotificationPolicy:(c:Client,body:any)=>req<any>(c,"/security-camera/notification-policies",json("POST",body)),notificationQueue:(c:Client,status?:string)=>req<NotificationQueueRow[]>(c,`/security-camera/notification-queue${query({status,limit:250})}`),notificationProviders:(c:Client)=>req<NotificationProviders>(c,"/security-camera/notification-providers"),inbox:(c:Client,limit=150)=>req<SecurityInboxNotice[]>(c,`/security-camera/inbox${query({limit})}`),markInboxRead:(c:Client,id:string)=>req<any>(c,`/security-camera/inbox/${id}/read`,json("POST",{})),serverUpdatePolicies:(c:Client)=>req<ServerUpdatePolicy[]>(c,"/security-camera/server-update-policies"),saveServerUpdatePolicy:(c:Client,serverId:string,body:any)=>req<any>(c,`/security-camera/servers/${serverId}/update-policy`,json("POST",body)),bulkFleet:(c:Client,body:any)=>req<{ok:boolean;action:string;requested:number;changed:number}>(c,"/security-camera/fleet/bulk",json("POST",body)),redundancy:(c:Client)=>req<RedundancyData>(c,"/security-camera/redundancy"),readiness:(c:Client)=>req<DeploymentReadiness>(c,"/security-camera/readiness"),recoveryDrill:(c:Client)=>req<RecoveryDrill>(c,"/security-camera/recovery-drill",json("POST",{})),saveRedundancy:(c:Client,cameraId:string,body:{primaryServerId:string;secondaryServerId:string;failoverAfterSeconds:number;failbackEnabled:boolean;failbackAfterSeconds:number;enabled:boolean})=>req<any>(c,`/security-camera/cameras/${cameraId}/redundancy`,json("POST",body)),manualFailover:(c:Client,cameraId:string,target:"primary"|"secondary",reason:string)=>req<{cameraId:string;state:string;serverId:string}>(c,`/security-camera/cameras/${cameraId}/failover`,json("POST",{target,reason})),recordings:(c:Client,cameraId?:string,limit=200)=>req<Recording[]>(c,`/security-camera/recordings${query({cameraId,limit})}`),liveSessions:(c:Client)=>req<LiveSession[]>(c,"/security-camera/live"),liveSession:(c:Client,id:string)=>req<LiveSession>(c,`/security-camera/live/${id}`),startLive:(c:Client,cameraId:string)=>req<LiveSession>(c,`/security-camera/cameras/${cameraId}/live`,json("POST",{})),endLive:(c:Client,id:string)=>req<any>(c,`/security-camera/live/${id}/end`,json("POST",{})),assign:(c:Client,cameraId:string,serverId:string|null)=>req<any>(c,`/security-camera/cameras/${cameraId}/assign`,json("POST",{serverId})),setRecording:(c:Client,cameraId:string,enabled:boolean)=>req<any>(c,`/security-camera/cameras/${cameraId}/recording`,json("POST",{enabled})),revoke:(c:Client,cameraId:string)=>req<any>(c,`/security-camera/cameras/${cameraId}/revoke`,json("POST",{})),operations:(c:Client)=>req<Operations>(c,"/security-camera/operations"),profile:(c:Client,cameraId:string)=>req<CameraProfile>(c,`/security-camera/cameras/${cameraId}/profile`),saveProfile:(c:Client,cameraId:string,body:any)=>req<CameraProfile>(c,`/security-camera/cameras/${cameraId}/profile`,json("POST",body)),timeline:(c:Client,cameraId:string,from:string,to:string,limit=3000)=>req<Timeline>(c,`/security-camera/cameras/${cameraId}/timeline${query({from,to,limit})}`),playback:(c:Client,recordingId:string)=>req<PlaybackGrant>(c,`/security-camera/recordings/${encodeURIComponent(recordingId)}/playback`,json("POST",{})),exportRange:(c:Client,cameraId:string,from:string,to:string)=>req<PlaybackGrant>(c,`/security-camera/cameras/${cameraId}/export`,json("POST",{from,to})),ackAlert:(c:Client,id:string)=>req<any>(c,`/security-camera/alerts/${id}/acknowledge`,json("POST",{})),events:(c:Client,p:{cameraId?:string;type?:string;status?:string;from?:string;to?:string;limit?:number}={})=>req<EventRow[]>(c,`/security-camera/events${query({...p,limit:p.limit||300})}`),eventConfig:(c:Client)=>req<EventConfig>(c,"/security-camera/events/config"),incidents:(c:Client)=>req<Incident[]>(c,"/security-camera/incidents"),saveZone:(c:Client,cameraId:string,body:any)=>req<any>(c,`/security-camera/cameras/${cameraId}/zones`,json("POST",body)),saveSchedule:(c:Client,body:any)=>req<any>(c,"/security-camera/event-schedules",json("POST",body)),saveRule:(c:Client,body:any)=>req<any>(c,"/security-camera/event-rules",json("POST",body)),reviewEvent:(c:Client,id:string,status:string)=>req<any>(c,`/security-camera/events/${id}/review`,json("POST",{status})),createIncident:(c:Client,id:string,body:any)=>req<any>(c,`/security-camera/events/${id}/incident`,json("POST",body)),evidence:(c:Client,id:string)=>req<{url:string;expiresAt?:string}>(c,`/security-camera/events/${id}/evidence`,json("POST",{}))};
