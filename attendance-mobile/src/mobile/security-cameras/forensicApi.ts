import type{MobileSession}from"../auth";import{ledgerlyRequest,type SessionUpdater}from"../apiClient";import type{CustodyEntry,ForensicCamera,ForensicHold,ForensicManifest}from"./forensicTypes";
type Client={session:MobileSession;onSession?:SessionUpdater};const req=<T>(c:Client,path:string,init:RequestInit={})=>ledgerlyRequest<T>(c.session,path,init,c.onSession);const post=<T>(c:Client,path:string,body:any)=>req<T>(c,path,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(body)});
export const securityCameraForensicApi={cameras:(c:Client)=>req<ForensicCamera[]>(c,"/security-camera/cameras"),holds:(c:Client)=>req<ForensicHold[]>(c,"/security-camera/forensics/holds"),custody:(c:Client)=>req<CustodyEntry[]>(c,"/security-camera/forensics/custody"),manifest:(c:Client,id:string)=>req<ForensicManifest>(c,`/security-camera/forensics/exports/${encodeURIComponent(id)}/manifest`),createHold:(c:Client,input:any)=>post(c,"/security-camera/forensics/holds",input),releaseHold:(c:Client,id:string,reason:string)=>post(c,`/security-camera/forensics/holds/${encodeURIComponent(id)}/release`,{reason}),createExport:(c:Client,cameraId:string,input:any)=>post<{id:string;url:string;manifestSha256:string;signingStatus:string;sourceSegments:number}>(c,`/security-camera/cameras/${encodeURIComponent(cameraId)}/forensic-export`,input),verifyCopy:(c:Client,id:string,contentSha256:string)=>post<{matched:boolean;status:string;expectedSha256:string;observedSha256:string}>(c,`/security-camera/forensics/exports/${encodeURIComponent(id)}/verify-copy`,{contentSha256})};
