import type{MobileSession}from"../auth";import{ledgerlyRequest,type SessionUpdater}from"../apiClient";import type{SupplyDashboard}from"./suppliesTypes";
type Client={session:MobileSession;onSession?:SessionUpdater};const req=<T>(c:Client,path:string,init:RequestInit={})=>ledgerlyRequest<T>(c.session,path,init,c.onSession);const json=(method:string,body?:unknown):RequestInit=>({method,headers:{"Content-Type":"application/json"},body:body===undefined?undefined:JSON.stringify(body)});const movement=(c:Client,id:string,body:any)=>{const type=String(body?.movementType||"");if((type==="writeoff"||type==="adjustment")&&!String(body?.notes||"").trim())return Promise.reject(new Error("Write-offs and stock adjustments require an explanatory note."));return req<any>(c,`/printerly/supplies/items/${id}/movements`,json("POST",body))};
export const printerlySuppliesApi={dashboard:(c:Client)=>req<SupplyDashboard>(c,"/printerly/supplies"),createItem:(c:Client,body:any)=>req<any>(c,"/printerly/supplies/items",json("POST",body)),updateItem:(c:Client,id:string,body:any)=>req<any>(c,`/printerly/supplies/items/${id}`,json("PUT",body)),deactivateItem:(c:Client,id:string)=>req<any>(c,`/printerly/supplies/items/${id}`,{method:"DELETE"}),addMovement:movement,saveMaintenance:(c:Client,printerId:string,body:any)=>req<any>(c,`/printerly/supplies/printers/${printerId}/maintenance`,json("PUT",body)),recordMaintenance:(c:Client,printerId:string,body:any)=>req<any>(c,`/printerly/supplies/printers/${printerId}/service`,json("POST",body)),sweep:(c:Client)=>req<any>(c,"/printerly/supplies/sweep",json("POST",{}))};
