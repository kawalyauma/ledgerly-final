import {Platform} from "react-native";
import {CameraSpool,OfflineStore} from "../../native";
import {DEFAULT_LEDGERLY_API_URL} from "../config";
import type {CameraApplianceHealth,CameraConfig,CameraProfile,CameraRegistration,CameraRemediationState,CameraRuntimeState,CameraStreamConfig} from "./types";
const REG_KEY="camera.device.registration";
export class CameraApiError extends Error{constructor(public status:number,public code:string,message:string){super(message);this.name="CameraApiError"}}
function base(path:string){return`${DEFAULT_LEDGERLY_API_URL.replace(/\/$/,"")}/api/v1/security-camera${path}`}
function headers(reg:CameraRegistration,body=false){return{Accept:"application/json",Authorization:`Device ${reg.deviceId}.${reg.credential}`,...(body?{"Content-Type":"application/json"}:{})}}
async function data<T>(response:Response):Promise<T>{const payload=await response.json().catch(()=>({})) as {data?:T;error?:{code?:string;message?:string}};if(!response.ok||payload.data===undefined)throw new CameraApiError(response.status,payload.error?.code||"CAMERA_API_ERROR",payload.error?.message||`Camera service returned ${response.status}`);return payload.data}
export function isCameraCredentialError(error:unknown){return error instanceof CameraApiError&&error.status===401||error instanceof Error&&/camera authentication|credential|revoked/i.test(error.message)}
export async function pairCamera(raw:string,deviceFingerprint:string){const response=await fetch(base("/device/pair"),{method:"POST",headers:{Accept:"application/json","Content-Type":"application/json"},body:JSON.stringify({token:raw,deviceFingerprint,platform:Platform.OS,appVersion:"0.13.0",capabilities:["video-capture","qr-pairing","heartbeat","whip-publish","whep-live","segmented-mp4-fallback","offline-segment-queue","bounded-offline-spool","appliance-diagnostics","health-remediation","thermal-hysteresis","low-battery-protection","capture-watchdog","android-appliance","remote-capture-profile","managed-boot-recovery"]})});return data<CameraRegistration>(response)}
export async function cameraHealth(reg:CameraRegistration){return data<{deviceId:string;status:string;serverTime:string}>(await fetch(base("/device/health"),{headers:headers(reg)}))}
export async function cameraConfig(reg:CameraRegistration){return data<CameraConfig>(await fetch(base("/device/config"),{headers:headers(reg)}))}
export async function cameraProfile(reg:CameraRegistration){return data<CameraProfile>(await fetch(base("/device/profile"),{headers:headers(reg)}))}
export async function cameraStreamConfig(reg:CameraRegistration){return data<CameraStreamConfig>(await fetch(base("/device/stream-config"),{headers:headers(reg)}))}
export async function cameraStreamState(reg:CameraRegistration,status:"online"|"connecting"|"offline"|"failed",message?:string){return data<{ok:boolean;status:string;serverTime:string}>(await fetch(base("/device/stream-state"),{method:"POST",headers:headers(reg,true),body:JSON.stringify({status,message})}))}
export async function cameraDiagnostics(reg:CameraRegistration,runtime?:CameraRuntimeState,appliance?:CameraApplianceHealth,remediation?:CameraRemediationState){const stats=await CameraSpool.stats().catch(()=>({count:runtime?.pendingSegments||0,bytes:runtime?.spoolBytes||0,freeBytes:runtime?.spoolFreeBytes||0,totalBytes:0,pressure:Boolean(runtime?.bufferPressure)}));return data<{ok:boolean;deviceId:string;serverTime:string}>(await fetch(base("/device/diagnostics"),{method:"POST",headers:headers(reg,true),body:JSON.stringify({pendingSegments:stats.count,spoolBytes:stats.bytes,spoolFreeBytes:stats.freeBytes,bufferPressure:stats.pressure,charging:appliance?.charging,thermalStatus:appliance?.thermalStatus,deviceOwner:appliance?.deviceOwner,applianceRunning:appliance?.running,wakeLock:appliance?.wakeLock,capturePaused:remediation?.capturePaused,pauseReason:remediation?.pauseReason,remediationCount:remediation?.remediationCount,lastRemediationAt:remediation?.lastRemediationAt,nvrReachability:remediation?.nvrReachability})}))}
export async function cameraHeartbeat(reg:CameraRegistration,runtime?:CameraRuntimeState,appliance?:CameraApplianceHealth,remediation?:CameraRemediationState){const heartbeat=await data<{ok:boolean;serverId?:string|null;serverAssigned:boolean;serverTime:string}>(await fetch(base("/device/heartbeat"),{method:"POST",headers:headers(reg,true),body:JSON.stringify({appVersion:"0.13.0",platform:Platform.OS,pendingSegments:runtime?.pendingSegments||0,recording:Boolean(runtime?.recording),batteryLevel:appliance?.batteryLevel,temperatureC:appliance?.temperatureC,thermalStatus:appliance?.thermalStatus,charging:appliance?.charging})}));void cameraDiagnostics(reg,runtime,appliance,remediation).catch(()=>{});return heartbeat}
export async function readCameraRegistration():Promise<CameraRegistration|null>{const raw=await OfflineStore.getSecure(REG_KEY);if(!raw)return null;try{return JSON.parse(raw) as CameraRegistration}catch{return null}}
export async function saveCameraRegistration(reg:CameraRegistration){await OfflineStore.putSecure(REG_KEY,JSON.stringify(reg))}
export async function clearCameraRegistration(){await OfflineStore.removeSecure(REG_KEY)}
