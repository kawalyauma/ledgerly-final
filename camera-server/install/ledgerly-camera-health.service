[Unit]
Description=Verify Ledgerly Camera Server health and recover if needed
After=ledgerly-camera.service

[Service]
Type=oneshot
ExecStart=/bin/sh -c '/usr/bin/curl --fail --silent --max-time 5 http://127.0.0.1:8789/health >/dev/null || /usr/bin/systemctl restart ledgerly-camera.service'
